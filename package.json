{
  "name": "@didley/decimal-input",
  "version": "1.0.3",
  "description": "Util for dealing with decimal inputs fields",
  "main": "dist/decimalInput.js",
  "types": "dist/decimalInput.d.ts",
  "scripts": {
    "build": "rm -rf ./dist && tsc --build",
    "tsWatch": "tsc --watch",
    "lint": "eslint .",
    "test": "vitest",
    "coverage": "vitest --coverage",
    "ci": "$npm_execpath lint && $npm_execpath build && $npm_execpath test --run"
  },
  "keywords": [
    "decimal",
    "input",
    "util",
    "float",
    "number",
    "frontend",
    "react"
  ],
  "author": "Dylan Lamont (didley)",
  "license": "MIT",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.2.0",
    "@typescript-eslint/parser": "^6.2.0",
    "@vitest/coverage-v8": "^0.33.0",
    "eslint": "^8.46.0",
    "jest": "^29.6.2",
    "typescript": "^5.1.6",
    "vitest": "^0.33.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/didley/decimal-input.git"
  },
  "bugs": {
    "url": "https://github.com/didley/decimal-input/issues"
  }
}